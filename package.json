{
  "name": "rox-next",
  "private": true,
  "version": "0.1.0",
  "description": "ROX-Next — an online multiplayer 3D RPG vertical slice inspired by Ragnarok X: Next Generation",
  "type": "module",
  "workspaces": [
    "shared",
    "engine",
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c green,cyan \"npm:dev:server\" \"npm:dev:client\"",
    "dev:server": "npm -w @rox/server run dev",
    "dev:client": "npm -w @rox/client run dev",
    "build": "npm run gen:manifest && npm run copy:decoders && npm -w @rox/shared run build && npm -w @rox/engine run build && npm -w @rox/server run build && npm -w @rox/client run build",
    "start": "npm -w @rox/server run start",
    "test": "node scripts/smoke.mjs",
    "test:solo": "tsx scripts/solo.test.ts",
    "test:model": "tsx scripts/model.test.ts",
    "check:models": "tsx scripts/check-models.ts",
    "gen:model": "tsx scripts/gen-demo-model.ts",
    "gen:models": "tsx scripts/gen-models.ts",
    "gen:manifest": "tsx scripts/gen-model-manifest.ts",
    "copy:decoders": "tsx scripts/copy-decoders.ts",
    "bestiary": "tsx scripts/dump-bestiary.ts",
    "checklist": "tsx scripts/dump-model-checklist.ts"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "typescript": "^5.6.3"
  }
}
